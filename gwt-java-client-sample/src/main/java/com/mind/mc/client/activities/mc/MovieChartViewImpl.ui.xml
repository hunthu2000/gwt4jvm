<!DOCTYPE ui:UiBinder SYSTEM "http://dl.google.com/gwt/DTD/xhtml.ent">
<ui:UiBinder xmlns:ui="urn:ui:com.google.gwt.uibinder" xmlns:g="urn:import:com.google.gwt.user.client.ui">

    <ui:style>

        .view {
            position:           relative;
            padding-top:        100px;
            font:               14px sans-serif,arial;
        }

        .message {
            width:              350px;
            margin:             0 auto;
            text-align:         center;
        }

        .logoutButton {
            position:           absolute;
            top:                0;
            right:              0;
        }

    </ui:style>

    <g:HTMLPanel styleName="{style.view}">
        <div class="{style.message}">
            Movie Chart isn't complete web-application and it doesn't have GUI. Its purpose is just to show how to write load tests using GWT Java Client.
        </div>
        <g:Button styleName="{style.logoutButton}" ui:field="logoutButton">Logout</g:Button>
    </g:HTMLPanel>

</ui:UiBinder>